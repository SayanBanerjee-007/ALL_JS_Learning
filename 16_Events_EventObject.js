document.getElementById('heading').addEventListener('click', function (temp) {
    console.log('Heading has been clicked');
    // location.href = 'https://www.codewithharry.com';
    let con;
    // con = temp.target;
    // con = temp.target.className;
    // con = temp.target.classList;
    // con = Array.from(temp.target.classList);
    // con = temp.target.id;
    
    // con = temp.offsetX;
    // con = temp.offsetY;
    
    // con = temp.clientX;
    // con = temp.clientY;

    console.log(con);

})